import { FC } from "react";
import NewItemForm from "../components/NewItemForm";

const Sell: FC = () => {
  return (
    <main>
      <h2>Sell Page</h2>
      <NewItemForm />
    </main>
  );
};

export default Sell;
